package com.example.kotlinguide

/** val are immutable, read-only local variables that can’t be reassigned.
 *
 *  Variables var that can be reassigned. These are mutable variables, and we can change their values after initialization.
 *
 *  Kotlin supports type inference and automatically identifies the data type of a declared variable. When declaring a variable, we can omit the type after the variable name.
 *
 *  Reference : [constructor-docs](https://www.simplilearn.com/tutorials/kotlin-tutorial/guide-to-kotlin-constructors)
 *  */
fun main() {
    val x: Int = 5
    // x=41

    var y: Int = 5


    var t = 5
}
